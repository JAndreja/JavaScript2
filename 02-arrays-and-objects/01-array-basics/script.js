"use strict";

const numbers = [3, 4, 6, 10];
console.log(numbers);
console.log(numbers.length);
const names = new Array("Andreja", "Radica", "Kaja");
console.log(names, typeof names);
console.log(names.pop());
